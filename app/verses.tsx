import { StaticInfoScreen } from "@/features/profile/screens/MoreScreens";

export default function VersesScreen() {
  return (
    <StaticInfoScreen
      title="Versets"
      text="Les versets favoris et les partages bibliques seront raccordes ici dans un ecran natif dedie."
    />
  );
}

import { StaticInfoScreen } from "@/features/profile/screens/MoreScreens";

export default function AdminRoute() {
  return (
    <StaticInfoScreen
      title="Administration"
      text="TODO: migrer le tableau admin en listes natives avec contrôle du rôle admin via Supabase."
    />
  );
}
